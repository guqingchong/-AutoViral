# Build Resources

Place the following files here before running `npm run build:installer`:

- `icon.ico` — Windows application icon (256x256 or multi-size ICO)
- `installer.nsh` — optional custom NSIS script fragments

If `icon.ico` is missing, electron-builder will use a default Electron icon.
